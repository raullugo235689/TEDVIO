import{createClient}from'https://esm.sh/@supabase/supabase-js@2';const c=window.TEDVIO_CONFIG||{},db=createClient(c.SUPABASE_URL,c.SUPABASE_PUBLISHABLE_KEY);const token=new URLSearchParams(location.search).get('t')||'',input=document.querySelector('#enrollment'),btn=document.querySelector('#checkin'),status=document.querySelector('#status');function show(msg,ok=false){status.textContent=msg;status.className='status '+(ok?'ok':'bad')}if(!token){show('Este código de asistencia no es válido. Vuelve a escanear el QR proyectado.');btn.disabled=true}btn.onclick=async()=>{const enrollment=input.value.trim();if(!enrollment)return show('Escribe tu matrícula.');btn.disabled=true;btn.textContent='Registrando…';try{const{data,error}=await db.rpc('v2_public_attendance_checkin',{p_token:token,p_enrollment:enrollment});if(error)throw error;if(!data?.ok)return show(data?.message||'No fue posible registrar la asistencia.');show(`${data.message}. ${data.student_name||''}`,true);input.disabled=true;btn.textContent='Asistencia registrada'}catch(e){show(e.message||'No fue posible registrar la asistencia.');btn.disabled=false;btn.textContent='Registrar asistencia'}};input.addEventListener('keydown',e=>{if(e.key==='Enter')btn.click()});