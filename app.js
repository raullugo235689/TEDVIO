const config = window.TEDVIO_CONFIG || {};
const app = document.querySelector('#app');

const state = {
  session: { code:'948271', title:'Biología Celular', teacher:'Prof. Ana Morales', connected:30, status:'live' },
  question: { id:'q1', prompt:'¿Cuál es la función principal de los lisosomas?', options:[['A','Producción de energía'],['B','Digestión intracelular'],['C','Síntesis de proteínas'],['D','Transporte de oxígeno']], correct:'B', results:{A:8,B:78,C:10,D:4} }
};
function logo(){return `<img class="logo" src="./assets/tedvio-logo.png" alt="TEDVIO" onerror="this.outerHTML='<div class=logo-fallback>TEDVIO</div>'">`;}
function qrPattern(){let seed=948271,cells='';for(let i=0;i<169;i++){seed=(seed*9301+49297)%233280;let on=seed/233280>.52;const x=i%13,y=Math.floor(i/13);if((x<3&&y<3)||(x>9&&y<3)||(x<3&&y>9))on=(x===0||y===0||x===2||y===2);cells+=`<i class="${on?'on':''}"></i>`;}return `<div class="qr-grid">${cells}</div>`;}
function teacher(){app.innerHTML=`<div class="shell"><header class="topbar">${logo()}<div class="topbar-actions"><span class="badge live"><i class="dot"></i> Sesión en vivo</span><button class="btn secondary" onclick="go('join')">Vista alumno</button></div></header><div class="layout"><aside class="sidebar"><div class="nav-title">TEDVIO LIVE</div><div class="nav"><button class="active">Resumen</button><button>Sesiones</button><button>Preguntas</button><button>Participantes</button><button>Analytics</button><button>Configuración</button></div></aside><main class="content"><section class="hero"><div><h1>${state.session.title}</h1><p>Interacción educativa en tiempo real · ${state.session.teacher}</p></div><button class="btn primary" onclick="newQuestion()">+ Nueva pregunta</button></section><section class="grid metrics"><div class="card metric"><small>Estudiantes conectados</small><strong>${state.session.connected}</strong><span>● en tiempo real</span></div><div class="card metric"><small>Participación</small><strong>86%</strong><span>+8% vs. anterior</span></div><div class="card metric"><small>Respuestas</small><strong>26</strong><span>de ${state.session.connected}</span></div><div class="card metric"><small>Tiempo de sesión</small><strong>02:34</strong><span>activa</span></div></section><section class="grid split"><div class="card"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center"><div><span class="badge" style="background:#eef4ff;color:#1e5bff">Pregunta en vivo</span><h2>${state.question.prompt}</h2></div><button class="btn secondary">Cerrar respuestas</button></div>${state.question.options.map(([k,v])=>`<div class="result-row"><div class="result-head"><span>${k}. ${v} ${k===state.question.correct?'<b class=correct>✓</b>':''}</span><b>${state.question.results[k]}%</b></div><div class="bar"><i style="width:${state.question.results[k]}%"></i></div></div>`).join('')}</div><div class="card session-panel"><span class="badge live"><i class="dot"></i> EN VIVO</span><h3>Únete a la sesión</h3><div class="code">${state.session.code}</div><p style="color:#cbd8f0">Escanea el QR o ingresa el código desde el celular.</p><div class="qr">${qrPattern()}</div></div></section></main></div></div>${config.DEMO_MODE?'<div class=demo>MVP visual · backend pendiente</div>':''}`;}
function join(){app.innerHTML=`<div class="join-wrap"><div class="join-card"><div class="join-brand">${logo()}<p style="color:#667085">Interacción educativa en tiempo real</p></div><h2>Únete a la sesión</h2><div class="field"><label>Código de sesión</label><input id="code" maxlength="6" value="${state.session.code}"></div><div class="field"><label>Nombre</label><input id="name" placeholder="Tu nombre"></div><button class="btn primary" style="width:100%" onclick="joinSession()">Entrar a la clase</button><button class="btn ghost" style="width:100%;margin-top:10px" onclick="go('teacher')">← Volver al profesor</button></div></div>`;}
function student(){app.innerHTML=`<div class="mobile-question"><div class="mobile-head">${logo()}<span class="badge live"><i class="dot"></i> Conectado</span></div><div class="mobile-body"><small style="color:#93b7ff">${state.session.title} · Pregunta 3 de 8</small><h1>${state.question.prompt}</h1><div id="student-options">${state.question.options.map(([k,v])=>`<button class="mobile-option" onclick="selectAnswer(this,'${k}')"><b>${k}</b> &nbsp; ${v}</button>`).join('')}</div><button id="send" class="btn primary" style="width:100%;margin-top:18px" onclick="sendAnswer()" disabled>Enviar respuesta</button></div></div>`;}
let selected=null;window.selectAnswer=(el,k)=>{selected=k;document.querySelectorAll('.mobile-option').forEach(x=>x.classList.remove('selected'));el.classList.add('selected');document.querySelector('#send').disabled=false;};window.sendAnswer=()=>{document.querySelector('.mobile-body').innerHTML=`<div style="text-align:center;padding-top:70px"><div style="font-size:64px">✓</div><h1>¡Respuesta enviada!</h1><p style="color:#b8c8e5">Tu respuesta <b>${selected}</b> fue registrada.</p><p>Espera la siguiente pregunta.</p></div>`;};window.joinSession=()=>go('student');window.newQuestion=()=>alert('Siguiente módulo del MVP: editor y lanzamiento de preguntas.');window.go=route=>{location.hash=route;render();};function render(){const route=(location.hash||'#teacher').slice(1);if(route==='join')join();else if(route==='student')student();else teacher();}window.addEventListener('hashchange',render);render();