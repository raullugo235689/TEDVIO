-- Recovered from the production migration ledger for deterministic rebuilds.
create table if not exists public.v2_paper_exams (id uuid primary key default gen_random_uuid(), teacher_id uuid not null, group_id uuid null references public.v2_groups(id) on delete set null, title text not null, subject text null, question_count int not null check(question_count between 1 and 60), option_count int not null default 4 check(option_count between 2 and 5), versions text[] not null default array['A']::text[], answer_keys jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now()); create table if not exists public.v2_paper_exam_results (id uuid primary key default gen_random_uuid(), exam_id uuid not null references public.v2_paper_exams(id) on delete cascade, teacher_id uuid not null, student_id uuid null references public.v2_group_students(id) on delete set null, enrollment text null, student_name text null, version text not null default 'A', answers jsonb not null default '[]'::jsonb, correct_count int not null default 0, blank_count int not null default 0, score numeric(5,2) not null default 0, reviewed boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz not null default now()); alter table public.v2_paper_exams enable row level security; alter table public.v2_paper_exam_results enable row level security; drop policy if exists v2_paper_exams_owner on public.v2_paper_exams; create policy v2_paper_exams_owner on public.v2_paper_exams for all to authenticated using (teacher_id=auth.uid()) with check (teacher_id=auth.uid()); drop policy if exists v2_paper_results_owner on public.v2_paper_exam_results; create policy v2_paper_results_owner on public.v2_paper_exam_results for all to authenticated using (teacher_id=auth.uid()) with check (teacher_id=auth.uid()); create unique index if not exists v2_paper_result_one_per_student on public.v2_paper_exam_results(exam_id,student_id,version) where student_id is not null;

