(()=>{
  const VERSION='2026.08.28.76.3';
  const ROUTES=new Set(['dashboard','bank','quizzes','history']);
  const R={installed:false,base:null,token:0,pending:'',current:'dashboard',bankDelegating:false,historyMode:'',scroll:new Map(),stage:null,announcer:null,lastReady:0};
  const core=()=>window.__TEDVIO_TEACHER686__||null;
  const app=()=>document.querySelector('#betaApp');
  const shell=()=>app()?.querySelector('.tv686-app')||null;
  const main=()=>shell()?.querySelector('.tv686-main')||null;
  const frame=()=>new Promise(resolve=>requestAnimationFrame(resolve));
  const routeLabel={dashboard:'Inicio',bank:'Banco',quizzes:'Preparadas',history:'Historial'};

  function routeFromButton(button){const code=button?.getAttribute?.('onclick')||'';return code.match(/betaView\(['\"]([^'\"]+)['\"]\)/)?.[1]||''}
  function routeFromLocation(){const raw=String(location.hash||'').replace(/^#/,'');const route=raw.startsWith('route=')?raw.slice(6):raw;return ROUTES.has(route)?route:'dashboard'}
  function routeUrl(view){const url=new URL(location.href);url.hash=view==='dashboard'?'':`route=${view}`;return`${url.pathname}${url.search}${url.hash}`}
  function ensureAnnouncer(){if(R.announcer?.isConnected)return R.announcer;let node=document.querySelector('#tv763RouteAnnouncer');if(!node){node=document.createElement('div');node.id='tv763RouteAnnouncer';node.className='tv763-sr-only';node.setAttribute('aria-live','polite');node.setAttribute('aria-atomic','true');document.body.appendChild(node)}R.announcer=node;return node}
  function announce(message){const node=ensureAnnouncer();node.textContent='';requestAnimationFrame(()=>{node.textContent=message})}
  function navButtons(){return[...document.querySelectorAll('#betaApp .tv686-top .b-top-actions button')].map(button=>({button,route:routeFromButton(button)})).filter(item=>ROUTES.has(item.route))}
  function mark(view=core()?.state?.view||R.current||'dashboard'){const active=ROUTES.has(view)?view:'dashboard';document.documentElement.dataset.tedvioRoute=active;navButtons().forEach(({button,route})=>{button.dataset.tv763Route=route;if(route===active)button.setAttribute('aria-current','page');else button.removeAttribute('aria-current');if(R.pending&&route===R.pending)button.setAttribute('aria-busy','true');else button.removeAttribute('aria-busy')})}
  function ready(view){const root=app(),content=main();if(!root||!content)return false;if(view==='dashboard')return!!content.querySelector('#tv686Dashboard .tv70-dashboard')&&!core()?.state?.busy;if(view==='bank')return!!content.querySelector('#qs65Root');const text=content.textContent||'';if(view==='quizzes')return/PREPARADAS/i.test(text)&&/Sesiones preparadas/i.test(text);if(view==='history')return/HISTORIAL/i.test(text)&&/Sesiones/i.test(text);return true}
  function begin(view){const token=++R.token;R.pending=view;document.documentElement.dataset.tedvioRouteBusy='true';main()?.setAttribute('aria-busy','true');mark(view);announce(`Abriendo ${routeLabel[view]||view}.`);return token}
  function finish(view,token){if(token!==R.token)return;R.pending='';R.current=view;delete document.documentElement.dataset.tedvioRouteBusy;main()?.removeAttribute('aria-busy');mark(view);announce(`${routeLabel[view]||view} listo.`);window.dispatchEvent(new CustomEvent('tedvio:route-ready',{detail:{view,version:VERSION}}))}
  function fail(token){if(token!==R.token)return;R.pending='';R.historyMode='';delete document.documentElement.dataset.tedvioRouteBusy;main()?.removeAttribute('aria-busy');mark(core()?.state?.view||R.current)}
  function saveScroll(view){if(ROUTES.has(view))R.scroll.set(view,{x:window.scrollX||0,y:window.scrollY||0})}
  async function restoreScroll(view){const point=R.scroll.get(view)||{x:0,y:0};await frame();await frame();window.scrollTo(point.x,point.y)}
  function commitHistory(view){const mode=R.historyMode||'push';R.historyMode='';const state={...(history.state||{}),tedvioRoute:view},url=routeUrl(view);if(mode==='none')return;if(mode==='replace')history.replaceState(state,'',url);else if(routeFromLocation()!==view)history.pushState(state,'',url);else history.replaceState(state,'',url)}
  function waitUntil(predicate,token,timeout=2600){return new Promise(resolve=>{const started=performance.now();let stable=0;const tick=()=>{if(token!==R.token)return resolve(false);let ok=false;try{ok=!!predicate()}catch{ok=false}stable=ok?stable+1:0;if(stable>=2)return resolve(true);if(performance.now()-started>=timeout)return resolve(ok);requestAnimationFrame(tick)};requestAnimationFrame(tick)})}
  function dashboardTemplate(){return`<section class="b-hero tv-dash-hero tv686-hero tv70-legacy-hidden"><div><span class="tv686-kicker">CENTRO DOCENTE</span><h1>Panel docente</h1><p>Tu operación académica en una vista rápida.</p></div><button class="b-btn primary" onclick="betaNewSession()">Crear sesión en vivo</button></section><div class="tv-quick-actions tv686-quick tv70-legacy-hidden"><button class="tv-quick primary" onclick="betaNewSession()"><span>＋</span><div><b>Nueva sesión</b><small>Inicia una actividad en vivo</small></div></button><button class="tv-quick" onclick="tvPilotOpenGroups()"><span>✓</span><div><b>Grupos</b><small>Alumnos y asistencia</small></div></button><button class="tv-quick" onclick="betaView('bank')"><span>▤</span><div><b>Banco</b><small>Crea y organiza reactivos</small></div></button><button class="tv-quick" onclick="tvPilotOpenExamForGroup()"><span>◎</span><div><b>Exámenes</b><small>OMR y resultados</small></div></button></div><section id="tv686Dashboard"><div class="tv686-loading"><span></span><b>Preparando tu resumen…</b></div></section>`}
  function removeStage(){R.stage?.remove();R.stage=null}
  async function stageDashboard(token){const c=core();let live=main();if(!c)return false;c.state.sessionCore?.leave?.();c.state.view='dashboard';if(!live){c.renderShell?.();await frame();live=main();if(!live)return false;Promise.resolve(c.refresh?.()).catch(error=>console.error('TEDVIO v76.3 dashboard refresh',error));return waitUntil(()=>ready('dashboard'),token,3000)}removeStage();const stage=document.createElement('main');stage.id='tv763DashboardStage';stage.className='b-main tv686-main tv763-dashboard-stage';stage.setAttribute('aria-hidden','true');stage.innerHTML=dashboardTemplate();document.body.appendChild(stage);R.stage=stage;window.dispatchEvent(new CustomEvent('tedvio:teacher-shell',{detail:{version:VERSION,persistent:true,stage:true}}));if(c.state.dashboard)window.dispatchEvent(new CustomEvent('tedvio:profile',{detail:c.state.profile||{}}));const refresh=Promise.resolve(c.refresh?.()).catch(error=>console.error('TEDVIO v76.3 dashboard refresh',error));await waitUntil(()=>stage.querySelector('#tv686Dashboard .tv70-dashboard')&&!c.state.busy,token,3200);if(token!==R.token){removeStage();return false}const children=[...stage.childNodes];live.replaceChildren(...children);removeStage();await refresh;return ready('dashboard')}
  async function openBank(token,throughWrapper){const c=core();if(c)c.state.view='bank';const loader=window.__TEDVIO_PROGRESSIVE_BOOT68__;if(!loader?.ensure)throw new Error('El cargador de funciones todavía no está disponible.');const ok=await loader.ensure('bank');if(ok===false||token!==R.token)return false;if(!throughWrapper&&window.betaView!==navigate){R.bankDelegating=true;try{window.betaView('bank')}finally{R.bankDelegating=false}}else if(!throughWrapper&&window.betaView===navigate){const api=window.__TEDVIO_QUESTION_STUDIO65__;if(api?.open)api.open();else if(api?.refresh)api.refresh()}return waitUntil(()=>ready('bank'),token,4200)}
  async function openCoreRoute(view,args,token){const result=R.base.call(window,view,...args);await Promise.resolve(result);return waitUntil(()=>ready(view),token,3200)}
  async function navigate(view='dashboard',...args){if(!ROUTES.has(view))return R.base?.call(this,view,...args);if(R.bankDelegating&&view==='bank'){if(core())core().state.view='bank';return}if(R.pending)return;const throughWrapper=window.betaView!==navigate,previous=core()?.state?.view||R.current||'dashboard';if(view===previous&&ready(view)){R.historyMode='';mark(view);return}saveScroll(previous);const token=begin(view);try{let ok=false;if(view==='dashboard')ok=await stageDashboard(token);else if(view==='bank')ok=await openBank(token,throughWrapper);else ok=await openCoreRoute(view,args,token);if(token!==R.token)return;if(!ok&&!ready(view))throw new Error(`La vista ${routeLabel[view]||view} no terminó de cargar.`);commitHistory(view);await restoreScroll(view);finish(view,token)}catch(error){console.error('TEDVIO v76.3 persistent router',view,error);fail(token);announce(`No se pudo abrir ${routeLabel[view]||view}.`);throw error}}
  function routeClick(event){const button=event.target.closest?.('#betaApp .tv686-top .b-top-actions button'),route=routeFromButton(button);if(!ROUTES.has(route))return;event.preventDefault();event.stopImmediatePropagation();if(R.pending)return;if(route===(core()?.state?.view||R.current)&&ready(route)){mark(route);return}Promise.resolve(window.betaView(route)).catch(error=>console.error('TEDVIO v76.3 top navigation',error))}
  function install(){if(R.installed)return true;const c=core(),base=window.betaView;if(!c||typeof base!=='function')return false;if(base.__tv763){R.installed=true;return true}R.base=base.__tv762Original||base.__tv761Original||base;navigate.__tv763=true;navigate.__tv763Original=R.base;window.betaView=navigate;R.installed=true;R.current=ROUTES.has(c.state?.view)?c.state.view:'dashboard';history.scrollRestoration='manual';document.addEventListener('click',routeClick,true);window.addEventListener('tedvio:teacher-shell',()=>requestAnimationFrame(()=>mark(core()?.state?.view||R.current)));window.addEventListener('tedvio:feature-ready',()=>requestAnimationFrame(()=>mark(core()?.state?.view||R.current)));window.addEventListener('popstate',()=>{R.historyMode='none';const target=routeFromLocation();Promise.resolve(window.betaView(target)).catch(error=>console.error('TEDVIO v76.3 history route',error))});mark(R.current);const requested=routeFromLocation();history.replaceState({...(history.state||{}),tedvioRoute:requested},'',routeUrl(requested));if(requested!==R.current){R.historyMode='replace';setTimeout(()=>Promise.resolve(window.betaView(requested)).catch(error=>console.error('TEDVIO v76.3 initial route',error)),0)}return true}
  function boot(attempt=0){if(install())return;if(attempt<100)setTimeout(()=>boot(attempt+1),50)}
  window.__TEDVIO_ROUTER763__={version:VERSION,open:view=>{R.historyMode='push';return Promise.resolve(window.betaView(view))},ready,mark,get current(){return R.current},get pending(){return R.pending},get shell(){return shell()}};
  boot();
})();

(()=>{
  const VERSION='2026.08.28.76.3';
  const FEATURE_BUTTONS={groups:'tvLazyGroups',tasks:'tvLazyTasks',omr:'tvLazyOmr',insight:'tvLazyInsight',help:'tvLazyHelp',onboarding:'tvLazySetup',admin:'tvLazyAdmin'};
  const V={surface:null,surfaceToken:0,groupInstalled:false,layoutShift:0,largestShift:0,busy:new Map(),sessionWatch:0};
  const frame=()=>new Promise(resolve=>requestAnimationFrame(resolve));
  const copyFunctionProps=(target,source)=>{for(const key of Object.keys(source||{})){try{target[key]=source[key]}catch{}}return target};

  function buttonForFeature(name){const id=FEATURE_BUTTONS[name];return id?document.getElementById(id):null}
  function clearButton(button){if(!button)return;button.removeAttribute('disabled');button.removeAttribute('aria-busy');button.removeAttribute('data-tv763-busy');const timer=V.busy.get(button);if(timer)clearTimeout(timer);V.busy.delete(button)}
  function markButton(button){if(!button)return;button.setAttribute('aria-busy','true');button.dataset.tv763Busy='true';const old=V.busy.get(button);if(old)clearTimeout(old);V.busy.set(button,setTimeout(()=>clearButton(button),8000))}
  function clearFeature(name){clearButton(buttonForFeature(name))}
  function clearAllButtons(){Object.values(FEATURE_BUTTONS).forEach(id=>clearButton(document.getElementById(id)))}

  function removeSurface(){V.surface?.remove();V.surface=null;delete document.documentElement.dataset.tedvioSurfaceBusy}
  function showSurface(label,detail='Conservando la vista hasta que el contenido esté listo.'){removeSurface();const wrap=document.createElement('div');wrap.id='tv763SurfaceHold';wrap.setAttribute('role','status');wrap.setAttribute('aria-live','polite');wrap.innerHTML=`<div class="tv763-hold-card"><span>TEDVIO</span><b>${String(label||'Preparando vista')}</b><small>${String(detail||'')}</small></div>`;document.body.appendChild(wrap);document.documentElement.dataset.tedvioSurfaceBusy='true';V.surface=wrap;return wrap}
  function waitStable(predicate,timeout=5000){return new Promise(resolve=>{const started=performance.now();let stable=0;const tick=()=>{let ok=false;try{ok=!!predicate()}catch{ok=false}stable=ok?stable+1:0;if(stable>=2)return resolve(true);if(performance.now()-started>=timeout)return resolve(ok);requestAnimationFrame(tick)};requestAnimationFrame(tick)})}
  async function releaseSurface(token){if(token!==V.surfaceToken)return;await frame();await frame();if(token!==V.surfaceToken)return;removeSurface()}
  async function withSurface(label,task,ready,timeout=6000){const token=++V.surfaceToken;showSurface(label);try{const result=await task();await waitStable(ready,timeout);return result}finally{await releaseSurface(token)}}

  function groupsReady(){const overlay=document.querySelector('#gaOverlay');if(!overlay)return false;const title=overlay.querySelector('h2')?.textContent||'';return/Mis grupos/i.test(title)&&(overlay.dataset.gaPremium==='1'||!!overlay.querySelector('.ga-premium-body')||!!overlay.querySelector('.ga-premium-modal'))}
  function groupCenterReady(){const overlay=document.querySelector('#gaOverlay');if(!overlay)return false;return!!overlay.querySelector('.ga360-top')&&!!overlay.querySelector('.ga360-body')&&!/Cargando Centro de grupo/i.test(overlay.textContent||'')}

  function installGroupStability(){if(V.groupInstalled)return;const back=window.gaBack,open=window.gaOpenGroup;if(typeof back!=='function'||typeof open!=='function')return;
    const stableBack=copyFunctionProps(async function(...args){return withSurface('Abriendo grupos…',()=>back.apply(this,args),groupsReady,5000)},back);stableBack.__tv763=true;
    const stableOpen=copyFunctionProps(async function(...args){return withSurface('Abriendo centro del grupo…',()=>open.apply(this,args),groupCenterReady,8000)},open);stableOpen.__tv763=true;
    window.gaBack=stableBack;window.gaOpenGroup=stableOpen;V.groupInstalled=true;
  }

  function interceptLegacyGroupButton(event){const button=event.target.closest?.('#tedvioGroupsBtn');if(!button||!V.groupInstalled)return;event.preventDefault();event.stopImmediatePropagation();Promise.resolve(window.gaBack?.()).catch(error=>{removeSurface();console.error('TEDVIO v76.3 grupos',error)})}

  function watchShellTransition(label,selector,before,delay=120,timeout=5000){const token=++V.sessionWatch;setTimeout(async()=>{if(token!==V.sessionWatch)return;const current=document.querySelector('#betaApp')?.firstElementChild;if(current&&current!==before&&current.matches(selector))return;const surfaceToken=++V.surfaceToken;showSurface(label);await waitStable(()=>{const node=document.querySelector('#betaApp')?.firstElementChild;return!!node&&node!==before&&node.matches(selector)},timeout);await releaseSurface(surfaceToken)},delay)}
  function sessionTransitionClick(event){const target=event.target.closest?.('#tv686CreateSession,[onclick*="betaOpenSession"],[onclick*="betaBackDashboard"],[onclick*="betaOpenReport"]');if(!target)return;const before=document.querySelector('#betaApp')?.firstElementChild;if(target.matches('[onclick*="betaBackDashboard"]'))watchShellTransition('Volviendo al centro docente…','.tv686-app',before,30,4500);else watchShellTransition('Preparando la sesión…','.tv686-session-shell',before,180,6000)}

  function installLayoutShiftProbe(){if(!('PerformanceObserver'in window))return;try{const observer=new PerformanceObserver(list=>{for(const entry of list.getEntries()){if(entry.hadRecentInput)continue;const value=Number(entry.value||0);V.layoutShift+=value;V.largestShift=Math.max(V.largestShift,value)}});observer.observe({type:'layout-shift',buffered:true})}catch{}}

  document.addEventListener('click',event=>{const lazy=event.target.closest?.('#tvLazyGroups,#tvLazyTasks,#tvLazyOmr,#tvLazyInsight,#tvLazyHelp,#tvLazyAdmin');if(lazy)markButton(lazy)},true);
  document.addEventListener('click',interceptLegacyGroupButton,true);
  document.addEventListener('click',sessionTransitionClick,true);
  window.addEventListener('tedvio:feature-ready',event=>{const name=event.detail?.name||'';clearFeature(name);if(name==='groups')requestAnimationFrame(installGroupStability)});
  window.addEventListener('tedvio:route-ready',clearAllButtons);
  window.addEventListener('tedvio:teacher-shell',clearAllButtons);
  window.addEventListener('pageshow',()=>{clearAllButtons();removeSurface()});
  document.addEventListener('visibilitychange',()=>{if(!document.hidden){clearAllButtons();if(!document.documentElement.dataset.tedvioSurfaceBusy)removeSurface()}});
  installLayoutShiftProbe();
  if(window.gaBack&&window.gaOpenGroup)installGroupStability();
  document.documentElement.dataset.tedvioVisualStability='763';
  window.__TEDVIO_VISUAL763__={version:VERSION,clearAllButtons,installGroupStability,showSurface,removeSurface,snapshot:()=>({layoutShift:Number(V.layoutShift.toFixed(4)),largestShift:Number(V.largestShift.toFixed(4)),surfaceBusy:!!V.surface,groupInstalled:V.groupInstalled})};
})();
